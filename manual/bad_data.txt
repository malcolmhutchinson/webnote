Bad data and something else
========

These files should contain all manner of errors. They can be used to test specific violations of the simple syntax.

Page 1 should gnerate a "paired directory not found" in the `webnote.Page` object.

The directory `missing_page` is missing it's paired page. It should not list in the contents below.
